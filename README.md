# Slot-Book
Built a full-featured generic slot booking app with React + Context API, featuring an interactive seat availability map, multi-step booking flow, form validation, and an admin dashboard with booking management. Implemented deterministic seat state generation to simulate real availability without a backend.
